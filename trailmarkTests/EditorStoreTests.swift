import Foundation
import Testing
import ComposableArchitecture
@testable import trailmark

// TODO: These tests need to be updated to match the current EditorStore API
// They were disabled because EditorStore was refactored (cursor, rename, delete moved to sub-features)
