# Matchfile

# Git repository URL for storing certificates and provisioning profiles
# Shared with Holfy (same Apple Developer account)
git_url("REDACTED_MATCH_URL")

# Storage mode
storage_mode("git")

# App Identifier
app_identifier(["com.nbarb.trailmark"])

# Your Apple Developer Team ID
team_id("REDACTED_TEAM_ID")

# Username (Apple ID email)
username("nicolas.barb.pro@gmail.com")

# Platform
platform("ios")

# Readonly mode (set to true for CI)
readonly(false)

# Force recreate if profiles are expired
force_for_new_devices(true)
